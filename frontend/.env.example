# Optional. This is the single backend base URL used by
# every frontend API call. The same local URL is used as
# the fallback when no frontend environment file exists.
VITE_API_BASE_URL=http://127.0.0.1:8000
